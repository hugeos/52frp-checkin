# FRP 自动签到项目 — 开发交付说明（给接手模型）

> **用途**：本文档供下一个 AI 模型接手开发时阅读。读完即可完全理解项目、避免已踩过的坑、直接继续开发。
> **项目代号**：52frp-checkin（GitHub: `hugeos/52frp-checkin`）
> **最后更新**：2026-07-27，最新可用 commit `5d78095`

---

## 一、项目目标

每天**自动**给 `52frp.com` 和 `88frp.com`（两个 FRP 内网穿透服务）签到，签到结果通过 **PushPlus** 推送到微信。

- 部署在 **Cloudflare Worker**，电脑关机也能跑，**零服务器成本**
- 支持**随机时间签到**（每天北京时间 8:00–22:45 之间随机一个时刻，且每天不同）
- 多站点隔离：只配了哪个站点的账号就签哪个，互不干扰
- 纯 API 实现，**不需要浏览器、不需要验证码**

---

## 二、系统架构

```
┌─────────────┐     Cron 每15分钟      ┌──────────────────┐
│ Cloudflare  │ ─────────────────────▶ │  Worker (index.js)│
│   Cron      │   "0,15,30,45 * * * *" │                  │
└─────────────┘                        │  1. 算当天幸运时间槽│
                                       │  2. 命中才签到      │
┌─────────────┐   GET /run            │  3. 并行签到多站点 │
│  手动触发    │ ─────────────────────▶ │                  │
│ (浏览器/API) │                        └────────┬─────────┘
└─────────────┘                                 │
                                        ┌───────┴────────┐
                                        ▼                ▼
                                 ┌────────────┐   ┌────────────┐
                                 │  52frp API │   │  88frp API │
                                 │ www.52frp  │   │  api.88frp │
                                 └────────────┘   └────────────┘
                                        │
                                        ▼
                                 ┌────────────┐
                                 │  PushPlus  │ ──▶ 微信推送
                                 │ (可选)     │
                                 └────────────┘
```

---

## 三、文件结构（改之前先看这个）

```
D:\Project\qiandao\
├── src/
│   ├── index.js         Worker 入口：Cron 调度 + 随机时间算法 + HTTP 手动触发 + 多站点并行
│   ├── lib.js           52frp 签到逻辑（API 客户端 + 响应解析 + PushPlus 推送）
│   └── frp88.js         88frp 签到逻辑（带 5xx 重试 + 安全 JSON 解析）
├── wrangler.toml        Worker 配置（name / main / cron / 注释里的 secret 说明）
├── package.json         仅 wrangler 一个 devDependency
├── .dev.vars.example    Secrets 示例（本地调试用，不要提交真实值）
├── .gitignore           忽略 .workbuddy/、node_modules/、debug.log、.dev.vars
├── 项目交接文档.md       早期交接文档（部分信息已滞后，以本文件为准）
├── 开发交付说明.md       本文件
└── test-local.js        本地测试脚本（设环境变量后 `node test-local.js`）
```

**不要新建重复文件**，所有功能已在这三个源文件里。改代码优先改对应模块。

---

## 四、★ 核心 API 契约（最重要，照抄，别改格式）

> 这两个契约是**逆向前端 JS 得到的事实**，已用真实账号验证通过。之前有 AI 改坏过 52frp 的提交格式，详见「七、已知坑」第 1 条。

### 4.1 52frp（`lib.js`）

| 步骤 | 方法 / 路径 | 请求体 | 说明 |
|------|------------|--------|------|
| 建 session | `GET https://www.52frp.com/user/` | — | 拿 cookie（代码里 `primeSession()` 自动做） |
| 登录 | `POST https://www.52frp.com/api/user/login` | JSON `{username, password}` | 返回 `data.token`，认证用 `Authorization: Bearer <token>` |
| 签到状态 | `GET https://www.52frp.com/api/user/sign/info` | — | 返回 `data.signed_today`（bool）等 |
| 滑块 token | `GET https://www.52frp.com/api/user/slider-token` | — | 返回一次性 `data.token` |
| **提交签到** | `POST https://www.52frp.com/api/user/sign` | **JSON `{slider_token: "..."}`** | **见下方红线** |
| 账户流量 | `GET https://www.52frp.com/api/user/info` | — | 可选，用于推送展示 |

**🚨 红线（提交签到格式）：**
- **必须是 `Content-Type: application/json`，body 只传 `{slider_token: "..."}`**
- **`csrf_token` 不要放进 body**。它由前端 axios 拦截器自动塞进 `Authorization: Bearer` 请求头（值就是登录拿到的 JWT），服务端从 header 取，不读 body 里的 csrf_token。
- 错误写法（曾经改坏过）：`application/x-www-form-urlencoded` + body 带 `csrf_token` → 服务端读不到 slider_token，返回「签到失败」。
- 前端真实代码：`aOt.post({url:"user/sign", data:{slider_token:e}})` —— 就这一种格式。

**登录/签到偶发滑块风险**：如果登录或签到时服务端要求真实滑块拖拽（纯 API 拿不到 token / 提交后仍 `signed_today:false`），属于无法纯 API 解决的场景。当前代码会抛出明确错误并记录日志。彻底方案是 GitHub Actions + Playwright 模拟拖拽（见「八、可选增强」）。

### 4.2 88frp（`frp88.js`）

| 步骤 | 方法 / 路径 | 请求体 | 说明 |
|------|------------|--------|------|
| 登录 | `POST https://api.88frp.com/api/auth/login` | JSON `{username, password}` | 返回 `data.token`（JWT），认证用 `Authorization: Bearer` |
| 用户信息 | `GET https://api.88frp.com/api/users/mine` | — | 含流量/余额/签到状态，**非关键** |
| 提交签到 | `POST https://api.88frp.com/api/domain/value-added-rights/signIn` | **无 body** | 已签返回 `message` 含「已签到」；成功返回 `data.success:true` |

**注意事项：**
- 必须带请求头 `Origin: https://www.88frp.com` 和 `Referer: https://www.88frp.com/`（实测不带也能登录，但带上更稳）
- 每次签到都重新登录拿新 token（JWT 有效期约 6 天，重登最稳，不存在 token 过期问题）
- **`api.88frp.com` 会间歇性返回 Cloudflare 522（源站超时）**，响应体是纯文本 `error code:522` → 必须用安全 JSON 解析（`safeJson()` 先 `.text()` 再 parse，非 JSON 返回 null），并对 5xx 做自动重试（最多 3 次，退避 1.5s→3s→4.5s）。这套已在 `frp88.js` 实现，不要删。

---

## 五、环境变量 / Secrets

全部通过 **Cloudflare Secrets** 配置（`wrangler secret put XXX`），**不要写进代码或 wrangler.toml**。

| 变量 | 必填 | 用途 | 缺失行为 |
|------|------|------|----------|
| `FRP_USERNAME` | 可选* | 52frp 账号 | 不配 → 跳过 52frp |
| `FRP_PASSWORD` | 可选* | 52frp 密码 | 不配 → 跳过 52frp |
| `FRP88_USERNAME` | 可选* | 88frp 账号 | 不配 → 跳过 88frp |
| `FRP88_PASSWORD` | 可选* | 88frp 密码 | 不配 → 跳过 88frp |
| `PUSHPLUS_TOKEN` | 可选 | PushPlus 微信推送 token | 不配 → 跳过推送，仅 console.log |
| `ACCESS_KEY` | 可选 | 保护 `GET /run` 手动触发（`?key=xxx`） | 不配 → `/run` 公开 |

\* 账号密码当前均为 `zimmar` / `Qq8381572388`（52frp 与 88frp 同号）。已在 Cloudflare Secrets 配好，本地测试用 `test-local.js` + 环境变量。

---

## 六、随机时间签到原理

Cloudflare Worker 有墙钟限制（约 10s），**无法长时间 sleep 等到指定时间**。改用「高频触发 + 随机命中」：

1. `wrangler.toml` 里 Cron 设为 `0,15,30,45 * * * *`（每 15 分钟一次，免费额度内）
2. 基于当天**北京时间**日期字符串算 **FNV-1a 哈希** → 取模得到当天稳定的「幸运时间槽」（0–59，对应北京时间 8:00–22:45）。**时间统一用 `getBeijingNow()` 转 UTC+8 计算，不要用裸 UTC，避免半夜签到。**
3. 每次 Cron 触发时，若当前北京时间槽位 >= 幸运槽位且在窗口内（8:00–22:45）才尝试签到；槽位 < 幸运槽位 或窗口外（深夜/凌晨）直接跳过
4. **KV 去重（关键，防次数超限）**：`SIGN_STATE` 命名空间记录 `signed:站点:北京日期`，当天成功签到后即写入，之后任何触发都不再调签到接口；失败也最多重试 `MAX_ATTEMPTS_PER_DAY=3` 次/天，超过当日放弃。这样无论网络怎么抖，单日调用签到 API 绝不会超过 3 次，**不会触发 52frp「签到次数上限」**

手动触发 `GET /run` 不受此限制，随时可执行；`GET /lucky` 查看当天幸运时间。

---

## 七、已知坑（接手必读）

| # | 坑 | 表现 | 正确处理 |
|---|----|------|----------|
| 1 | **52frp 签到提交格式** | 改成 form-urlencoded+body.csrf_token 后「签到失败」 | 必须用 JSON `{slider_token}` + `Authorization: Bearer`（见 4.1 红线）。已 revert 修复 |
| 2 | **88frp 522 错误** | 偶发 `error code:522` 致 `JSON.parse` 崩溃报「Unexpected token」 | `safeJson()` + 5xx 重试，已在 `frp88.js` |
| 3 | **`*.workers.dev` 国内不可达** | 国内浏览器/API 打不开 `xxx.workers.dev` | **正常，不是 Worker 挂了**。验证走：PushPlus 微信推送 / 境外网络 / 本地 `node test-local.js` |
| 4 | **52frp 滑块风险** | 登录或签到偶发要求真实滑块 | 纯 API 无法解决，记录日志 + 推送告警即可；彻底方案见「八」 |
| 5 | **`debug.log` 误入仓库** | 搜狗输入法日志混入 | 已加入 `.gitignore`，可删 |
| 6 | **`.workbuddy/` 勿动** | WorkBuddy 工作记忆目录 | 已 gitignore，别删、别提交 |
| 7 | **⚠️ 签到次数超限（曾发生）** | 旧代码"幸运时间后每15分钟重试"在签到失败时反复调 API，单日 ~56 次 → 打满 52frp「签到次数上限」→ 账号被锁 | **已用 KV 去重根治**：成功即记 `signed:站点:日期` 不再调接口；失败最多 3 次/天。改签到触发逻辑时**绝不可删 KV 去重**，否则必然复发超限 |
| 8 | **⚠️ 时区导致半夜签** | Worker 运行时是 UTC，裸用 `getHours()`/`toISOString()` 会把北京时间算错 | 已统一 `getBeijingNow()`（UTC+8）计算，窗口外返回 -1 跳过。新增时间判断**必须走 `getBeijingNow()`**，禁止裸 UTC |

---

## 八、当前状态 & 待办

**已完成（可稳定运行）：**
- ✅ 52frp + 88frp 双站点纯 API 签到
- ✅ 随机时间调度（Cron 每 15 分钟 + 幸运槽命中）
- ✅ PushPlus 微信推送（成功/已签/失败 三种状态）
- ✅ 88frp 522 重试 + 安全 JSON 解析
- ✅ **KV 去重 + 失败重试上限**（根治「签到次数超限」，2026-07-30 修复）
- ✅ 已部署（自定义域名 `https://frpsign.qaeb.com`，Worker `52frp-checkin.zima04.workers.dev`），KV 绑定 `SIGN_STATE` 已生效

**可选增强（按需做，别动核心）：**
1. 签到结果持久化（KV/D1 存历史，统计连续天数）
2. 52frp 滑块兜底：GitHub Actions + Playwright 模拟拖拽（仅滑块触发时启用）
3. 推送失败重试（当前失败只记日志）
4. 多站点通用化（新增站点只需写一个 `frpXX.js` 模块 + 在 `index.js` 注册）

---

## 九、部署 & 验证（给新模型的操作清单）

**修改代码后：**
```bash
git add . && git commit -m "..." && git push origin main
# Cloudflare 已连 GitHub，push 到 main 自动重新部署，无需手动操作
```

**本地验证（国内推荐，绕过 workers.dev 不可达问题）：**
```bash
cd D:/Project/qiandao
FRP_USERNAME=zimmar FRP_PASSWORD=Qq8381572388 \
FRP88_USERNAME=zimmar FRP88_PASSWORD=Qq8381572388 \
node test-local.js
```

**首次部署 / 改 Secrets（如需要）：**
```bash
npx wrangler secret put FRP_USERNAME
npx wrangler secret put FRP_PASSWORD
# ... 其余同理
npx wrangler deploy
```

**线上验证（需境外网络）：**
```bash
curl https://52frp-checkin.ks2ut3ehp2.workers.dev/run   # 手动触发
curl https://52frp-checkin.ks2ut3ehp2.workers.dev/lucky # 看今日幸运时间
```

---

## 十、给接手模型的明确指令

1. **改 API 请求格式前，先用 `curl` 直连接口 + 下载前端 JS 逆向确认契约**，不要凭字面猜测（csrf_token / x-www-form-urlencoded 字样容易误导）。
2. **不要删 `frp88.js` 的 `safeJson()` 和重试逻辑**，522 是真实存在的。
3. **账号密码只走 Secrets**，绝不写进代码/git。
4. 国内打不开 `*.workers.dev` 不算 bug，**用微信推送或本地脚本验证**。
5. 多站点已支持并行，新增站点照 `frp88.js` 写模块即可。
