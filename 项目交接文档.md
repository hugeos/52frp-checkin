# FRP 自动签到项目 — 交接文档

> 本文档供下一位开发者/AI 接手使用，涵盖项目全貌、技术细节、部署状态、已知问题与后续方向。

---

## 一、项目概述

每天自动签到 **52frp.com** 和 **88frp.com** 两个 FRP 内网穿透站点，签到结果通过 PushPlus 推送到微信。全程跑在 Cloudflare Worker 上，电脑关机也能签，完全免费。

| 项目 | 内容 |
|------|------|
| 本地路径 | `D:\Project\qiandao\` |
| GitHub 仓库 | `git@github.com:hugeos/52frp-checkin.git`（私有） |
| 线上 Worker | `https://52frp-checkin.ks2ut3ehp2.workers.dev`（⚠️ 国内访问不了，见下方备注） |
| 最新提交 | `5d78095` — revert(52frp): 还原 lib.js，修正换 AI 后的错误改动 |
| 技术栈 | Cloudflare Workers + Cron Trigger + PushPlus |
| 依赖 | 零运行时依赖，仅需 `wrangler`（devDependency） |
| 账号 | 52frp / 88frp 用户名均为 `zimmar` |

---

## 二、文件结构

```
D:\Project\qiandao\
├── src/
│   ├── index.js        Worker 入口：Cron 定时触发 + HTTP 手动触发 + 多站点调度
│   ├── lib.js          52frp 签到逻辑：API 客户端 + PushPlus 推送（316行）
│   └── frp88.js        88frp 签到逻辑：带重试的安全请求封装（239行）
├── test-local.js       本地测试脚本（部署前用真实账号验证 API 是否通）
├── wrangler.toml       Cloudflare Worker 配置（含 cron）
├── package.json        type: module，仅 wrangler 一个 devDependency
├── .dev.vars.example   本地开发密钥模板
├── .gitignore          忽略 node_modules / .dev.vars / .wrangler / .workbuddy
├── README.md           完整部署和使用说明
├── debug.log           搜狗输入法日志（无用，可删）
└── .workbuddy/         WorkBuddy 工作记忆（勿删，勿提交 git）
```

---

## 三、核心逻辑

### 3.1 随机时间签到机制

固定时间签到容易被识别为自动化。采用**多频触发 + 随机命中**方案：

- Cron 每 15 分钟触发一次（`0,15,30,45 * * * *`），每天 96 次
- `dailyHash()` 用 FNV-1a 对当天 UTC 日期字符串做哈希 → 取模 60 得到"幸运时间槽"
- 时间窗口：槽位 0-59 = 北京时间 8:00-22:45，每槽 15 分钟
- 只有命中幸运槽才真正签到，其余跳过（几乎不消耗额度）
- 每天签到时间不同，但同一天内稳定（不会重复签）
- 手动访问 `/run` 不受随机限制，随时可触发
- `/lucky` 接口可查看今日幸运时间

**为什么不用 sleep？** Cloudflare Worker 有墙钟时间限制（约 10s），无法长时间 sleep 等到目标时间。多频触发是绕过限制的标准做法。

### 3.2 多站点调度

`index.js` 的 `handleCheckin(env)` 按 `Promise.all` 并行签到所有已配置站点：

- 配了 `FRP_USERNAME` → 签 52frp
- 配了 `FRP88_USERNAME` → 签 88frp
- 都没配 → 返回错误
- 签完汇总结果，通过 PushPlus 推送

### 3.3 52frp 签到链路（`src/lib.js`）

逆向自 52frp 面板 API，纯 API 无需浏览器：

```
1. GET  /user/                  建立 session（拿 cookie）
2. POST /api/user/login         账号密码登录 → Bearer token
3. GET  /api/user/info          账户流量信息（可选，用于推送展示）
4. GET  /api/user/sign/info     查询今日签到状态
5. GET  /api/user/slider-token  获取一次性 slider_token
6. POST /api/user/sign          提交签到 { slider_token }
```

**关键点**：`slider_token` 由服务端下发，签到提交只需该 token，无需拖拽轨迹，所以纯 API 可行。

**风险**：登录环节偶尔触发滑块验证时纯 API 拿不到 token；服务端若改要求真实拖拽轨迹则 API 被拒。代码里有明确的 `throw new Error` 提示。备选方案：GitHub Actions + Playwright 浏览器自动化（社区方案 `doubletree6/52frp-checkin`）。

`lib.js` 的 API 客户端设计较完整：
- `createApiClient()` 封装了 cookie 存储、token 管理、自动 JSON 解析（try/catch 不崩溃）
- `unwrapData()` 处理 52frp API 多层嵌套 `data` 的特殊情况
- `extractMessage()` / `extractLoginToken()` / `extractSliderToken()` 容错提取字段
- `normalizeSignInfo()` / `normalizeUserInfo()` 标准化不同字段命名

### 3.4 88frp 签到链路（`src/frp88.js`）

逆向自 88frp 前端 JS（`/assets/index-xV52z6j5.js`），比 52frp 更简单：

```
1. POST /api/auth/login                         账号密码登录 → JWT token（无验证码！）
2. GET  /api/users/mine                         用户信息（流量、签到状态、余额）
3. POST /api/domain/value-added-rights/signIn   提交签到
```

**比 52frp 更可靠**：无滑块、无 captcha、纯账号密码 + JWT。JWT token 有效期 6 天，但代码每次签到都重新登录，不存在过期问题。

**健壮性设计**（2025-07-05 修复）：
- `safeJson()`：先 `.text()` 再安全 `JSON.parse`，非 JSON 响应（如 Cloudflare 522 错误页）返回 `null` 不崩溃
- `request88()`：封装 fetch，**5xx / 网络错误 / 超时自动重试**（最多 3 次，退避延迟 1.5s → 3s → 4.5s）
- `fetchWithTimeout()`：`AbortController` 实现 15s 超时，避免 Worker 挂死
- 登录 / 用户信息 / 签到统一走 `request88()`

> ⚠️ 这个修复的背景：`api.88frp.com` 间歇性返回 Cloudflare 522 错误（源站连接超时），响应体是纯文本 `error code:522`，旧代码直接 `.json()` 解析就崩了。

### 3.5 PushPlus 推送

`lib.js` 的 `sendPushPlus(env, title, content)` 调用 `https://www.pushplus.plus/send`，所有站点共用一个 `PUSHPLUS_TOKEN`。签到结果汇总后统一推送一条消息到微信。

---

## 四、环境变量 / Secrets

在 Cloudflare Worker 的 **Settings → Variables and Secrets** 中配置：

| 名称 | 必填 | 说明 |
|------|------|------|
| `FRP_USERNAME` | 签 52frp 必填 | 52frp 账号 |
| `FRP_PASSWORD` | 签 52frp 必填 | 52frp 密码 |
| `FRP88_USERNAME` | 签 88frp 必填 | 88frp 账号 |
| `FRP88_PASSWORD` | 签 88frp 必填 | 88frp 密码 |
| `PUSHPLUS_TOKEN` | 可选 | PushPlus token，所有站点共用，配了才推送微信 |
| `ACCESS_KEY` | 可选 | 自定义密钥，保护 `/run` 接口防被外人随意触发 |

本地开发用 `.dev.vars`（复制 `.dev.vars.example`），切勿提交到 git。

---

## 五、部署状态

| 项 | 状态 |
|----|------|
| 代码 | ✅ 完成，已推 GitHub |
| Cloudflare 部署 | ✅ 已部署（网页端连接 GitHub，push 到 main 自动重新部署） |
| Cron 触发器 | ✅ 每 15 分钟，已生效 |
| Secrets | ✅ `FRP_USERNAME` / `FRP_PASSWORD` 已配；`FRP88_*` 待确认；`PUSHPLUS_TOKEN` 待用户配置 |
| 线上验证 | ✅ 访问 `/run` 返回 `already_signed`，签到流程正常 |

**部署方式**：用户选择的是 Cloudflare 网页端 **Workers Builds 连接 GitHub 仓库**（非命令行 wrangler）。每次 push 到 `main` 分支，Cloudflare 自动拉取代码、`npm install`、`npx wrangler deploy`。

> 注意：如果修改了代码，push 到 GitHub 后 Cloudflare 会自动重新部署。也可以在 Cloudflare 网页端手动点 Retry deployment。

---

## 六、API 接口

| 路径 | 方法 | 说明 |
|------|------|------|
| `/run` 或 `/` | GET | 手动触发签到（不受随机时间限制）。配了 `ACCESS_KEY` 时需 `?key=xxx` |
| `/lucky` | GET | 查看今日幸运签到时间（不执行签到） |

返回示例：
```json
{
  "status": "success",
  "results": [
    { "site": "52frp", "status": "already_signed", "message": "52frp 连续签到4天 累计1.53GB 剩余331.17GB", "elapsed": "2.3s" },
    { "site": "88frp", "status": "already_signed", "message": "88frp 总流量1.35GB 余额¥142.70", "elapsed": "1.8s" }
  ],
  "push": "推送结果...",
  "luckyTime": "14:30"
}
```

---

## 七、本地开发

```bash
# 安装依赖
npm install

# 本地测试签到（部署前验证 API 是否通）
FRP_USERNAME=账号 FRP_PASSWORD=密码 node test-local.js
FRP88_USERNAME=账号 FRP88_PASSWORD=密码 node test-local.js

# 本地 wrangler dev（需先创建 .dev.vars）
cp .dev.vars.example .dev.vars
# 编辑 .dev.vars 填入账号密码
npm run dev

# 部署（需 wrangler login）
npm run deploy
```

---

## 八、已知问题与风险

| 问题 | 严重度 | 说明 | 应对 |
|------|--------|------|------|
| 52frp 登录偶发滑块验证 | 中 | 纯 API 无法拖滑块，当天签到失败 | 次日通常恢复；备选 GitHub Actions + Playwright |
| 88frp 间歇性 522 错误 | 低 | 已修复，自动重试 3 次 | 2025-07-05 已解决 |
| 52frp 无自动重试 | 低 | `lib.js` 有安全 JSON 解析但无 5xx 重试 | 如遇类似 522 问题，可参考 `frp88.js` 的 `request88()` 加重试 |
| PushPlus 免费版推送上限 | 低 | 每日推送次数有限 | 签到每天最多 1 条，不会超 |
| `debug.log` 无用文件 | 低 | 搜狗输入法日志，误入仓库 | 可删，已加入 .gitignore |

---

## 九、Git 提交历史

```
5d78095  revert(52frp): 还原 lib.js 签到逻辑，修正换 AI 后的错误改动
25f3b1d  fix(88frp): 修复522错误导致JSON解析崩溃，加入自动重试
db0f001  feat: 新增 88frp.com 签到支持
f0827e7  feat: 随机时间签到
3aee67d  feat: 52frp 自动签到 Cloudflare Worker 版
```

---

## 十、后续可优化方向

1. **52frp 加重试机制** — 参考 `frp88.js` 的 `request88()`，给 `lib.js` 的 API 客户端加 5xx 重试
2. **PushPlus 推送失败重试** — 当前推送失败只记日志，可加 1-2 次重试
3. **签到结果持久化** — 接 D1 或 KV 记录历史签到结果，方便统计连续签到天数
4. **更多站点** — 架构已支持多站点，新增站点只需写一个类似 `frp88.js` 的模块并在 `index.js` 注册
5. **GitHub Actions 备选** — 52frp 滑块问题彻底解决方案，用 Playwright 模拟浏览器拖拽
6. **签到失败告警** — 连续 N 天失败时推送不同级别的告警消息

---

## 十一、关键提醒给接手者

1. **账号密码**：52frp 和 88frp 都是 `zimmar` / `Qq8381572388`，已配在 Cloudflare Secrets 里。本地测试时通过环境变量传入。
2. **不要把账号密码写进代码或提交 git**，一律走环境变量 / Secrets。
3. **`.workbuddy/` 目录是 WorkBuddy 的工作记忆，不要删，也不要提交 git**（已在 .gitignore）。
4. **修改代码后**：push 到 GitHub `main` 分支，Cloudflare 会自动重新部署。也可以网页端手动 Retry。
5. **验证签到是否正常**：浏览器访问 `https://52frp-checkin.ks2ut3ehp2.workers.dev/run`，看 JSON 返回。
   ⚠️ **重要**：`*.workers.dev` 域名在**中国大陆网络环境下无法直接访问**（DNS/连接被阻断），这是正常现象，不代表 Worker 挂了。国内验证请改用以下方式之一：
   - 看 PushPlus 微信推送（每天签到后会推一条结果消息，最直观）
   - 用境外网络 / 代理访问 `/run` 看 JSON
   - 本地 `node test-local.js`（设好 `FRP_USERNAME` / `FRP_PASSWORD` 环境变量）跑整条链路
6. **两个站点独立**：只配一个站点的 Secrets，另一个自动跳过，互不影响。
